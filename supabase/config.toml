# ⚠️  DO NOT RUN `supabase config push` AGAINST THIS PROJECT.
#
# The CLI compares a local config.toml against its own BUILT-IN DEFAULTS, not
# against "leave undeclared properties alone". Pushing from here — even from a
# file declaring nothing but two URLs — was measured to make 11 changes to the
# hosted project, including disabling MFA TOTP, disabling Twilio SMS, changing
# OTP length from 8 to 6, and resizing the connection pooler. A non-interactive
# run proceeds without asking.
#
# Hosted auth settings are managed in the dashboard. Migrations are the only
# thing we push, via `supabase db push`.
#
# Run `supabase config diff` before ever reconsidering this.

project_id = "6-Degrees"

# Recorded here for reference only — set these in the dashboard, not from here:
#   Authentication → URL Configuration
#     Site URL:  https://edgeoinnovations-resources.github.io/6DegreesBeta/
#     Redirect:  https://edgeoinnovations-resources.github.io/6DegreesBeta/**
#                http://127.0.0.1:8000/**   (local development)
