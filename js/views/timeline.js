// View placeholder — replaced in next step.
import { el } from '../widgets.js';
export const view = {
  id: 'timeline', num: 6, title: 'Timeline',
  render(root){ root.appendChild(el('div.view-head',{},[el('h2',{text:'Timeline — building…'})])); },
};
