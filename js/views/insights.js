// View placeholder — replaced in next step.
import { el } from '../widgets.js';
export const view = {
  id: 'insights', num: 7, title: 'Insights',
  render(root){ root.appendChild(el('div.view-head',{},[el('h2',{text:'Insights — building…'})])); },
};
