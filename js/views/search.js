// View placeholder — replaced in next step.
import { el } from '../widgets.js';
export const view = {
  id: 'search', num: 8, title: 'Search',
  render(root){ root.appendChild(el('div.view-head',{},[el('h2',{text:'Search — building…'})])); },
};
